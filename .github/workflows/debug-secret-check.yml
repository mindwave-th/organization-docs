name: Debug Secret Check

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    env:
      MY_TOKEN: ${{ secrets.ORG_WORKFLOW_SYNC_PAT }}
    steps:
      - name: Report length only (never print the value)
        run: |
          echo "length: ${#MY_TOKEN}"
